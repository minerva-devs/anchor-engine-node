This is a verification test document for Anchor Engine.
It contains multiple sentences about semantic memory, graph traversal,
and the STAR algorithm. This content should appear in search results.

The Anchor Engine is a deterministic semantic memory system that uses
graph traversal instead of vector search. The STAR algorithm walks the
graph to find related concepts. Memory is stored as compounds, molecules,
and atoms. Atoms contain tags that represent concepts.