<!-- ARCHIVED: scripts README moved to archive/docs_removed/scripts/README.md -->

````markdown
# Scripts: anchor repo split & submodule migration

This folder contains scripts to help split `anchor/` into a standalone Git repository, push the split history to a new remote, and optionally replace the `anchor/` folder with a submodule.

<!-- Content preserved, trimmed here for archival reference -->
````
