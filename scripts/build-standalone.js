// Simple build script for standalone engine
console.log('Preparing standalone engine with built-in UI...');
console.log('Nothing to build for this simple HTML/JS UI - it\'s already in the public directory.');
console.log('The engine will serve the UI from the public directory when the external UI is not available.');