# 2026-05-25

## Persistent Memory

**Project:** Anchor Engine Node.js
- **Version:** 5.2.0
- **Working Directory:** `C:\Users\rsbii\Projects\anchor-engine-node`
- **Database:** PGlite embedded (PostgreSQL)

**Mission:** GitHub Repository Cleanup on Failed Syncs
- **PRD:** `C:\Users\rsbii\.qwenpaw\workspaces\P1\missions\mission-20260525-180647\prd.json`
- **Issue:** 101,355+ orphaned molecules from failed GitHub sync
- **Solution:** Auto-detect old atoms (>100 count) and clean up on next sync
- **Status:** ✅ All user stories passed

**GitHub Ingest Service:**
- Location: `engine/src/services/ingest/github-ingest-service.ts`
- Features implemented:
  - `getOldAtomCount(repoId)` - Detects failed syncs (101k+ atoms)
  - `cleanupOldRepoData(repoId, owner, repo, bucket)` - Deletes orphaned data
  - Modified `syncRepo()` - Auto-cleanup trigger

**UI Customization:**
- Changed GitHub icon color from gray to white in `engine/public/index.html`
- Fixed from `text-gray-400` to `text-white` with `hover:text-green-400` for better visibility
- Verified icon triggers `onOpenGithub={() => setShowGithub(true)}` → opens GitHub modal

**Testing Framework:**
- Created comprehensive dual-monitoring framework for frontend UI + backend logs
- Files: `TESTING-GITHUB-CLEANUP.md`, `verify-github-cleanup.bat`
- Log directory: `C:\Users\rsbii\.anchor\logs\` with script `pnpm start-with-logging`
- Testing workflow: Start server → Open UI → Click GitHub icon → Monitor `server.log` for cleanup activity

**Logging Configuration:**
- Updated `package.json` `start-with-logging` script to output to `.anchor/logs/anchor_engine.log` (was `../../logs/`)
- GitHub service uses `console.log` → appears in logs with `[GitHub]` prefix

**Docs Consolidation (Phase 1 & 2):**
- Deleted 10 redundant docs: `paper.md`, `API.md`, `mcp-agent.md`, installation docs, code/design patterns, references
- Moved 3 files to `specs/`: `STRUCTURE.md` → `ARCHITECTURE.md`, `TAXONOMY.md` → `DATA-MODEL.md`, `mcp-setup.md` → `INTEGRATIONS/MCP-INTEGRATION.md`
- Created `docs/onboarding.md` → single-file onboarding that points to 5 core docs
- Moved academic files to `docs/arxiv/`, testing docs to `docs/testing/`
- User insight: `specs/spec.md` is single source of truth for architecture + API → no duplicate API.md needed

## Experience Reflection

**Mission System Trigger:**
- ✅ User's "lets proceed" is the standard trigger to advance mission phase (PRD → execution)
- ✅ Agent must execute tasks immediately when user confirms "proceed"
- ✅ Fall back to direct API testing when agents fail to execute

**Debugging Workflow:**
- ✅ When shell commands fail (PowerShell issues), use direct file edits or alternative verification methods
- ✅ Mission system requires strict PRD format - always validate fields: `userStories` must have `id`, `title`, `description`, `priority`, `acceptanceCriteria`
- ✅ GitHub cleanup is implemented in code but needs real-time testing via UI or API monitoring
- ✅ Use grep/search tools to verify implementation before marking stories as passed

**User Feedback Integration:**
- ✅ User clarified "lets proceed" triggers mission confirmation (PRD → execution)
- ✅ User's "proceed" command is the standard trigger to advance the mission phase
- ✅ When agents fail to execute tasks, fall back to direct API testing or manual verification

**Testing Strategies:**
- ✅ For long-running processes (101k+ atom cleanup), monitor logs with `Get-Content engine.log -Tail 50` (PowerShell) or `tail -f` (Linux)
- ✅ Use API endpoints directly when UI is not accessible: `curl -X POST "http://localhost:3160/v1/github/repos` with proper JSON formatting
- ✅ PRD validation requires camelCase fields: `acceptanceCriteria` not `acceptance_criteria`

**Docs Organization Principle:**
- ✅ `specs/spec.md` is single source of truth for architecture + API → no duplicate API.md needed in docs/
- ✅ Flat docs/ structure with 5 core files: onboarding.md, whitepaper.md, testing/, arxiv/
- ✅ User insight: new agents can be onboarded with single file: `docs/onboarding.md`

**Organizational Cleanliness:**
- ✅ Prohibited files (CLEANUP_REPORT.md, test-coverage gaps, installation duplicates) must be deleted immediately
- ✅ Academic papers should be in docs/arxiv/, not root
- ✅ Integration docs in specs/INTEGRATIONS/, not scattered
- ✅ Always verify before executing destructive operations (del/rmdir)

**File Path Handling:**
- ✅ PowerShell commands require full paths with escaped spaces
- ✅ Use read_file tool to verify file content before deleting
- ✅ Directory listing with dir command requires /b flag for clean output
