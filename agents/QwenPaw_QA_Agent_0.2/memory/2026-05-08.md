# 2026-05-08 — 会话记忆与反思

## 持久化记忆

### 项目状态
- **ResearchAgent 工作区已创建**: `~/.qwenpaw/workspaces/ResearchAgent/`
  - 包含: agent.json, SOUL.md, skills/research/SKILL.md, research_reports/, README.md
  - 用途: 深度研究代理，使用 Tavily MCP + browser_use 进行网络搜索和文章阅读
  - 功能: 最多 50 次搜索/任务，引用标准，人工交接付费内容
  - 待办: 用户需要在 `agent.json` 中设置 Tavily API 密钥才能启用搜索功能

### 用户信息更新
- 用户希望简化 cron jobs，移除 fridgebot，仅保留 inventory bot
- 用户对研究代理感兴趣，希望它能够：
  - 使用 websearch 和 browser 进行研究
  - 正确引用来源（链接、文章、论文）
  - 提供摘要
  - 允许最多 50 次搜索，读取前 50 个结果，仅保留相关内容

### 智能体状态
- **Inventory Bot** (`SY5pnT`): 活跃，cron job 每日 8:00 AM 检查库存
- **VL Scanner** (`N8cD2T`): 活跃，cron job 每日 9:00 AM 扫描 VL 图片
- **Fridge Bot** (`nG5pFk`): 已废弃（~~deprecated~~）
- **ResearchAgent**: 新创建，待用户配置 Tavily API 后启用

### Cron Jobs
- `qwenpaw cron list --all` 返回空列表 `[]`
- 需要进一步调查 cron jobs 是否实际注册或仅存在于 MEMORY.md 中

## 经验反思

### 成功策略
1. **ResearchAgent 设计**:
   - 采用分层文件结构（agent.json, SOUL.md, SKILL.md）清晰定义代理行为
   - 在 SOUL.md 中明确定义研究工作流程（澄清→搜索→筛选→阅读→引用→综合）
   - 设置搜索预算（最多 50 次）和引用标准，确保研究质量
   - 人工交接机制：当文章付费或需要深度阅读时，提供链接和摘要供用户自行查看

2. **MEMORY.md 更新**:
   - 及时将新代理信息添加到 MEMORY.md，保持记忆同步

### 待改进/注意事项
1. **Cron Jobs 验证**:
   - 用户提到 cron jobs 时，`qwenpaw cron list` 返回空列表
   - 可能需要检查 cron jobs 是否实际注册，或是否存在于其他位置
   - 未来交互中应先验证 cron jobs 状态，而非仅依赖 MEMORY.md 中的记录

2. **用户反馈处理**:
   - 用户多次中断对话，提示需要更简洁、直接的回答
   - 避免过长解释，优先执行用户指令

3. **Tavily API 配置**:
   - ResearchAgent 创建后，用户尚未配置 Tavily API 密钥
   - 下次交互时应主动提醒用户完成此步骤

### 行动指南
- 用户关注 cron jobs 简化，下次应优先检查并清理不必要的 cron jobs
- ResearchAgent 需要 Tavily API 密钥才能工作，应提醒用户配置
- 保持回答简洁，避免过度解释
